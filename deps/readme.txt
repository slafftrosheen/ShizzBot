# Dependencies

This folder is intended for external, locally managed library dependencies that cannot be resolved via PlatformIO's built-in `lib_deps` registry.

For most libraries (like M5Unified, FastLED, or WiFiManager), prefer defining them in `platformio.ini`.
